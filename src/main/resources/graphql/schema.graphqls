type Query {
  authors: [Author]
  notesByAuthorId(authorId: ID): [Note]
}

type Author {
  id: ID
  firstName: String
  lastName: String
}

type Note {
  id: ID
  noteTitle: String
  noteContent: String
  author: Author
}