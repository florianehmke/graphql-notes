query notes ($userId: Long, $searchTerm: String) {
  notes(userId: $userId, searchTerm: $searchTerm) {
    id
    noteTitle
    noteContent
    createdBy {
      firstName
      lastName
    }
  }
}

